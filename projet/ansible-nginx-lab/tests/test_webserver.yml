---
- name: Test Webserver
  hosts: localhost
  gather_facts: no
  tasks:
    - name: Check if Nginx is responding
      uri:
        url: "http://192.168.56.10"
        status_code: 200
      register: result
      until: result.status == 200
      retries: 5
      delay: 2
